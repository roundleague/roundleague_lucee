# Copy this file to .env and keep it local — it is gitignored.
# These are local development credentials only. Never use production values here.

MYSQL_ROOT_PASSWORD=root_dev_password
MYSQL_USER=roundleague
MYSQL_PASSWORD=roundleague_dev
MYSQL_DATABASE=roundleague
